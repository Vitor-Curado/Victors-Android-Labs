<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VictorsAndroidLabs" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary brand colour. -->
        <item name="colorPrimary">@color/my_primary</item>

        <!-- Secondary brand colour. -->
        <item name="colorSecondary">@color/my_secondary</item>

        <!-- Status bar colour. -->
        <item name="android:statusBarColor">@color/my_statusbar</item>

        <!-- Text colour. -->
        <item name="android:textColor">@color/dark_gray</item>

    </style>

    <style name="Theme.VictorsAndroidLabs" parent="Base.Theme.VictorsAndroidLabs" />
</resources>